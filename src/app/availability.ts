/* Used to model availability by days of the week, availability is
  represented as a number becase ¯\_(ツ)_/¯ 
  Occasionally the compilter cannot find this file and I have no idea why? */

export class Availability {
  M: number;
  T: number;
  W: number;
  Th: number;
  F: number;
  S: number;
  Su: number;
}
